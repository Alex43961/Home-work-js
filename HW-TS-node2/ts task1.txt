на каждым заданием ставите коммент с номерром задания и рандомным юникодом еды

каждое задание выполняете в отдельном файле

----

0. настройте компилятор на удаление комментариев, авто компиляцию при изменении файлов и слияние файлов в 1 файл(называете его своей фамилией)
(настройки компилятора найдите в google)

1. создать функцию, принимающую 2 числа и возвращает их разницу.

2. скопировать функцию из 1го задания и сделать оба параметра необязательными, функция работает так же, но если оба параметра - undefined, то она выкидывает ошибку.
Если только втворой параметр - undefined, то вернет 1й в квадрате

3. функция принимает массив строк и возврващает, новый массив из значений полученного, приведя их к числу.

4*. функция получает объект с полями name, age, pet и выводит их значения в консоль, сделайте через деструктуризацию. 
(синтаксис найдите в google)

5*. создать функцию принимающую массив непонятно каких значений(generic) и возвращающая созданный из него Set

---

загружаете 1й папкой все ts файлы(4) + tsconfig.json + ваш bundle